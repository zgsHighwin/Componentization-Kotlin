ext {
    // 定义一个项目全局变量isRelease，用于动态切换：组件化模式 / 集成化模式
    // false: 组件化模式（子模块可以独立运行），true ：集成化模式（打包整个项目apk，子模块不可独立运行）
    isRelease = true


    androidId = [
            compileSdkVersion: 30,
            buildToolsVersion: "29.0.3",
            minSdkVersion    : 16,
            targetSdkVersion : 30,
            versionCode      : 1,
            versionName      : "1.0"
    ]

    //不同模块的包名
    appId = [
            "app"     : "com.savannah.componentizationproject",
            "order"   : "com.savannah.componentizationproject.order",
            "personal": "com.savannah.componentizationproject.personal",

    ]

    url = [
            "debug"  : "https://2.3.4.5/debug",
            "release": "https://1.2.3.4/release",
    ]

    kotlin_version = "1.4.10"


    dependenciesLib = [
            "kotlinstdlib"    : "org.jetbrains.kotlin:kotlin-stdlib:${kotlin_version}",
            "coreKtx"         : "androidx.core:core-ktx:1.3.2",
            "appcompat"       : "androidx.appcompat:appcompat:1.2.0",
            "constraintLayout": "androidx.constraintlayout:constraintlayout:2.0.2",
    ]

    testDependenciesLib = [
            "junit": "junit:junit:4.12",
    ]
    androidDependenciesLib = [
            "junitext"    : "androidx.test.ext:junit:1.1.2",
            "espressoCore": "androidx.test.espresso:espresso-core:3.3.0"
    ]
}